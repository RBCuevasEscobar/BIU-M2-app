{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Interfaz de usuario para la aplicación E-Commerce, desacoplada del backend.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node scripts/build-dist.js",
    "build:local": "npx @tailwindcss/cli -i ./css/styles.css -o ./css/output.css --minify",
    "watch": "npx @tailwindcss/cli -i ./css/styles.css -o ./css/output.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "autoprefixer": "^10.4.24",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18"
  }
}